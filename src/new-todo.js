// todo constructor
// properties: name,
// due date, priority, notes, checklist, parentList (with a default one)

export const toDo = (name, dueDate, priority, notes, checklist) => {
  return { name, dueDate, priority, notes, checklist };
};
